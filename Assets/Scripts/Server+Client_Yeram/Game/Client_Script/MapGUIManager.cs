using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapGUIManager : Singleton_Ver2.Singleton<MapGUIManager>
{

    #region test
    public void Click_InitTile()
    {
        MapManager.Instance.InitRequest();
    }
    #endregion
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
