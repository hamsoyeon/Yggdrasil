using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NewMenuManager : Singleton_Ver2.Singleton<MenuGUIManager>
{
    [SerializeField]
    GameObject m_MainMenu;
    [SerializeField]
    GameObject m_Room;
    [SerializeField]
    GameObject m_Option;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
